# Install with:
#   pip install --no-deps -r requirements.txt
#
# ultralytics normally pulls in opencv-python>=4.6.0, which would shadow
# the system cv2 4.5.4 (apt, GStreamer-enabled) inside this
# --system-site-packages venv. opencv-python is deliberately NOT listed
# here -- everything below was verified to work against system cv2 4.5.4.
#
# aarch64 note: `import torch` must happen before `cv2`/`gi` are imported
# anywhere in the process, or torch's bundled libgomp fails with
# "cannot allocate memory in static TLS block". See CLAUDE.md.

pyyaml
numpy>=1.23.0,<2.0.0
matplotlib>=3.4,!=3.6.1
pillow>=7.1.2
requests>=2.23.0
scipy>=1.4.1
tqdm>=4.64.0
psutil
py-cpuinfo
pandas>=1.2
seaborn>=0.11.0
ultralytics-thop>=2.0.0
torch==2.4.1
torchvision==0.19.1
lapx>=0.5.2
ultralytics==8.3.0

# TensorRT export path (pipeline/inference.py VehicleDetector's .engine backend).
# protobuf is bumped from the system 3.6.1 (too old for onnx's generated
# _pb2 modules, which need protobuf>=3.20's `builder` API) -- the venv copy
# shadows the system one and nothing else here needs the old version.
protobuf>=3.20.2
onnx>=1.17.0
ml_dtypes>=0.2.0
# ultralytics' ONNX export auto-installs/downgrades these two on first run
# (onnxslim==0.1.94 -> 0.1.34 to match its onnxruntime pin) -- pinned here
# so a fresh install lands on the same versions without a second pip pass.
onnxslim==0.1.34
onnxruntime==1.16.3
flatbuffers
coloredlogs
humanfriendly
# pycuda: built from source against /usr/local/cuda-11.4 (no aarch64 wheel,
# ~5-8min build). Needs CUDA_ROOT=/usr/local/cuda-11.4 and nvcc on PATH:
#   export CUDA_ROOT=/usr/local/cuda-11.4 PATH=/usr/local/cuda-11.4/bin:$PATH
# Pinned to 2024.1 -- 2025.x/2026.x pycuda.compyte.dtypes uses `str | Seq[str]`
# PEP 604 union syntax in a class body, which raises
# `TypeError: 'ABCMeta' object is not subscriptable` on Python 3.8.
pycuda==2024.1
